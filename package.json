{
  "name": "@deno/black-box-testing",
  "description": "Black box testing app",
  "type": "module",
  "scripts": {
    "spotify": "deno run --allow-env --allow-read --allow-write --allow-net --inspect-brk -c ./test-data/spotify.json > output.json",

    "led-eod-batch": "deno run --allow-env --allow-read --allow-write --allow-net --inspect-brk scenario-black-box.ts -w test-data -c batch-led-eod-config.json > output.json",
    "led-eod-batch-dry": "deno run --allow-env --allow-read --allow-write --allow-net --inspect-brk scenario-black-box.ts -w test-data -c batch-led-eod-config.json -e dry > output.json",

    "batch-free": "deno run --allow-env --allow-read --allow-write --allow-net --inspect-brk scenario-black-box.ts -w test-data -c batch-free-config.json > output.json"

  }
}
